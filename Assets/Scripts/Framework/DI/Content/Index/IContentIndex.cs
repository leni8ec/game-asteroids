namespace Asteroids.Framework.DI.Content.Index {

    // todo-consider: rename to 'repository' ? (however, it's still not a repository)
    /// <summary>
    /// Content index represents predefined instantiated objects,
    /// which can be added to DI or use directly
    /// </summary>
    /// <example> Usages of 'WorldStateIndex' in GUI </example>
    public interface IContentIndex { }

}